apiVersion: v1
kind: Service
metadata:
  name: {{ include "threetier-app.fullname" . }}
  labels:
    {{- include "threetier-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "threetier-app.selectorLabels" . | nindent 4 }}
